<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary color -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- Primary dark color -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!-- Accent color -->
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Background color -->
        <item name="android:windowBackground">@color/colorBackground</item>
    </style>

    <!-- Text styles -->
    <style name="TextStyleTitle">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TextStyleSubtitle">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textStyle">italic</item>
    </style>

    <!-- Custom chart background -->
    <style name="ChartBackground">
        <item name="chart_backgroundColor">@color/colorBackground</item>
    </style>

    <!-- Default styles for PieChart -->
    <style name="PieChartStyle" >
        <item name="chart_backgroundColor">@color/white</item>
    </style>

</resources>